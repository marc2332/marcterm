[package]
name = "marcterm"
edition = "2024"
publish = false
version = "0.1.0"

[[bin]]
name = "marcterm"
path = "src/main.rs"

[dependencies]
freya = { version = "0.4.0-rc.11", features = ["terminal", "radio"] }
serde = { version = "1", features = ["derive"] }
toml = "0.9"
dirs = "6"
