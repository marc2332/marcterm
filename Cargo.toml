[package]
name = "marcterm"
edition = "2024"
publish = false

[[bin]]
name = "marcterm"
path = "src/main.rs"

[dependencies]
freya = { git = "https://github.com/marc2332/freya", features = ["terminal", "radio"] }
serde = { version = "1", features = ["derive"] }
toml = "0.8"
dirs = "6"
